# Attributes:
# bold: makes text bold
# reverse: apparently reverses fg with bg and vice-versa
# standout: is apparently doing the same thing as reverse ???
# underline: does something expected of it. underlines.
# invis: apparently fg and bg goes black
# protect: no idea
# dim: didn't see shit. But probably dims colors
# blink: blinks, but doesn't seem to work as I thought

#auto-reload yes

external-url-viewer "urlview"

bind-key k up
bind-key j down
bind-key l open
bind-key h quit
bind-key g home
bind-key G end
bind-key j next articlelist
bind-key k prev articlelist
bind-key J next-feed articlelist
bind-key K prev-feed articlelist
bind-key u pageup
bind-key d pagedown

# Nuke Sniff's colorstyle that I have to fix the bg conflicts someday
# color listnormal cyan default
# color listfocus black yellow standout bold
# color listnormal_unread blue default
# color listfocus_unread yellow default bold
# color info red black bold
# color article white default bold

color listnormal        color244 default
color listfocus         black    default  bold
color listnormal_unread color10  default
color listfocus_unread  color46  default  bold
color info              color208 default  bold
color article           white    default  bold

highlight all      "---.*---"                          yellow
highlight feedlist ".*(0/0))"                          black
highlight article  "(^Feed:.*|^Title:.*|^Author:.*)"   cyan default bold
highlight article  "(^Link:.*|^Date:.*)"               default default
highlight article  "https?://[^ ]+"                    green default
highlight article  "^(Title):.*$"                      blue default
highlight article  "\\[[0-9][0-9]*\\]"                 magenta default bold
highlight article  "\\[image\\ [0-9]+\\]"              green default bold
highlight article  "\\[embedded flash: [0-9][0-9]*\\]" green default bold
highlight article  ":.*\\(link\\)$"                    cyan default
highlight article  ":.*\\(image\\)$"                   blue default
highlight article  ":.*\\(embedded flash\\)$"          magenta default
